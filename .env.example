APP_NAME="SIGAP Emergency API"APP_NAME="Emergency API"

APP_ENV=localAPP_ENV=local

APP_KEY=APP_KEY=

APP_DEBUG=trueAPP_DEBUG=true

APP_URL=http://localhostAPP_URL=http://localhost



APP_LOCALE=enAPP_LOCALE=en

APP_FALLBACK_LOCALE=enAPP_FALLBACK_LOCALE=en

APP_FAKER_LOCALE=en_USAPP_FAKER_LOCALE=en_US



APP_MAINTENANCE_DRIVER=fileAPP_MAINTENANCE_DRIVER=file

# APP_MAINTENANCE_STORE=database# APP_MAINTENANCE_STORE=database



PHP_CLI_SERVER_WORKERS=4PHP_CLI_SERVER_WORKERS=4



BCRYPT_ROUNDS=12BCRYPT_ROUNDS=12



LOG_CHANNEL=stackLOG_CHANNEL=stack

LOG_STACK=singleLOG_STACK=single

LOG_DEPRECATIONS_CHANNEL=nullLOG_DEPRECATIONS_CHANNEL=null

LOG_LEVEL=debugLOG_LEVEL=debug



# Database ConfigurationDB_CONNECTION=mysql

# For local development, use MySQL/MariaDBDB_HOST=127.0.0.1

# For production, configure your managed database (e.g., DigitalOcean Managed Database)DB_PORT=3306

DB_CONNECTION=mysqlDB_DATABASE=emergency_api

DB_HOST=127.0.0.1DB_USERNAME=emergency_user

DB_PORT=3306DB_PASSWORD=

DB_DATABASE=emergency_api

DB_USERNAME=rootSESSION_DRIVER=database

DB_PASSWORD=SESSION_LIFETIME=120

# DB_SSLMODE=required  # Uncomment for production with SSLSESSION_ENCRYPT=false

SESSION_PATH=/

SESSION_DRIVER=databaseSESSION_DOMAIN=null

SESSION_LIFETIME=120

SESSION_ENCRYPT=falseBROADCAST_CONNECTION=log

SESSION_PATH=/FILESYSTEM_DISK=local

SESSION_DOMAIN=nullQUEUE_CONNECTION=database



BROADCAST_CONNECTION=logFILESYSTEM_DISK=spaces

FILESYSTEM_DISK=local

QUEUE_CONNECTION=syncCACHE_STORE=database

# CACHE_PREFIX=

CACHE_STORE=database

# CACHE_PREFIX=MEMCACHED_HOST=127.0.0.1



MEMCACHED_HOST=127.0.0.1REDIS_CLIENT=phpredis

REDIS_HOST=127.0.0.1

REDIS_CLIENT=phpredisREDIS_PASSWORD=null

REDIS_HOST=127.0.0.1REDIS_PORT=6379

REDIS_PASSWORD=null

REDIS_PORT=6379# Email Configuration untuk Email Verification

# INSTRUKSI SETUP:

# Email Configuration# 1. Buat Gmail account untuk system

# Gunakan SMTP provider seperti Postmark, SendGrid, Gmail, atau Mailgun# 2. Enable 2FA di Gmail account

# # 3. Generate App Password (16 karakter)

# Untuk Postmark:# 4. Replace values di bawah dengan credentials asli

# 1. Daftar di https://postmarkapp.comMAIL_MAILER=smtp

# 2. Dapatkan API Token dari Server SettingsMAIL_HOST=smtp.gmail.com

# 3. Verify sender email addressMAIL_PORT=587

# 4. Isi credentials di bawahMAIL_USERNAME=your-email@gmail.com

#MAIL_PASSWORD=your-gmail-app-password

# Untuk Gmail:MAIL_ENCRYPTION=tls

# 1. Enable 2FA di Gmail accountMAIL_FROM_ADDRESS="noreply@sigap-emergency.com"

# 2. Generate App Password (16 karakter)MAIL_FROM_NAME="SIGAP Emergency API"

# 3. Gunakan MAIL_HOST=smtp.gmail.com, MAIL_PORT=587

MAIL_MAILER=smtp# WhatsApp Notification via Fonnte

MAIL_SCHEME=nullFONNTE_TOKEN=tgX3Wbv4zgGaVSDU749z

MAIL_HOST=smtp.postmarkapp.comFONNTE_BASE_URL=https://api.fonnte.com

MAIL_PORT=587

MAIL_USERNAME=your-postmark-api-token# DigitalOcean Spaces Configuration

MAIL_PASSWORD=your-postmark-api-tokenAWS_ACCESS_KEY_ID=your_spaces_access_key

MAIL_ENCRYPTION=tlsAWS_SECRET_ACCESS_KEY=your_spaces_secret_key

MAIL_FROM_ADDRESS="noreply@yourdomain.com"AWS_DEFAULT_REGION=sgp1

MAIL_FROM_NAME="${APP_NAME}"AWS_BUCKET=your-bucket-name

AWS_URL=https://your-bucket-name.sgp1.digitaloceanspaces.com

# WhatsApp Notification ConfigurationAWS_ENDPOINT=https://sgp1.digitaloceanspaces.com

# Gunakan Fonnte (https://fonnte.com) untuk WhatsApp notificationsAWS_USE_PATH_STYLE_ENDPOINT=false

# 1. Daftar di https://fonnte.com

# 2. Connect WhatsApp number AndaVITE_APP_NAME="${APP_NAME}"

# 3. Dapatkan API Token dari dashboard
# 4. Isi FONNTE_TOKEN di bawah
FONNTE_TOKEN=your-fonnte-api-token
FONNTE_BASE_URL=https://api.fonnte.com

# AWS/DigitalOcean Spaces Configuration (Optional)
# Untuk file storage di cloud (jika tidak pakai local storage)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"
